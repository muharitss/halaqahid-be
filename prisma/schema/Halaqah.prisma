enum JenisHalaqah {
  TAHFIDZ
  BACAAN
}

model Halaqah {
  id_halaqah   Int          @id @default(autoincrement())
  name_halaqah String
  muhafiz_id   Int          @unique
  
  jenis        JenisHalaqah @default(TAHFIDZ) 
  
  created_at   DateTime     @default(now())
  updated_at   DateTime     @updatedAt
  deleted_at   DateTime?

  user         User         @relation(fields: [muhafiz_id], references: [id_user])
  santri       Santri[]
}