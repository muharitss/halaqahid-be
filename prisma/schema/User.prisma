model User {
    id_user Int @id @default(autoincrement())
    username String @unique
    email String @unique
    password String
    role String @default("muhafiz")
    halaqah    Halaqah?

    deleted_at DateTime?

    absensiAsatidz AbsensiAsatidz[]

    @@map("user")
}